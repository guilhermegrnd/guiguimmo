export const GLOBAL_SERVER_URL = 'https://localhost:5007';
export const GAME_SERVER_URL = 'https://localhost:5003';
export const CHARACTERS_SERVER_URL = 'https://localhost:5005';

export const ISO_TILE_WIDTH = 64;  // W
export const ISO_TILE_HEIGHT = 32; // H (This is the visible diamond height)
export const ISO_HALF_WIDTH = ISO_TILE_WIDTH / 2;  // 32
export const ISO_HALF_HEIGHT = ISO_TILE_HEIGHT / 2; // 16

// Optional: Offset to center the map origin (0,0) in the container
export const SCREEN_CENTER_OFFSET_X = 200; 
export const SCREEN_CENTER_OFFSET_Y = 50;